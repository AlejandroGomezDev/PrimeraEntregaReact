import cart from "./cart3.svg"

function CartWidget() {
    return (
            <img className="h-9 mx-4 cursor-pointer" src={cart} alt="logo-carrito"></img>
            
    );
}

export default CartWidget;
